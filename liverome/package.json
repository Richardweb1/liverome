{
  "name": "liverome",
  "version": "0.1.0",
  "private": true,
  "description": "Live Adaptive Yield Engine - GenLayer Intelligent Contract",
  "scripts": {
    "lint:contract": "genvm-lint check contracts/liverome.py --json",
    "test:contract": "python -m pytest test/test_liverome.py -v",
    "deploy:studio": "ts-node deploy/deploy.ts studio",
    "deploy:bradbury": "ts-node deploy/deploy.ts bradbury"
  },
  "dependencies": {
    "genlayer-js": "latest"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4",
    "@types/node": "^20.14.0"
  }
}
